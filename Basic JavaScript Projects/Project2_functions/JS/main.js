function myFunction() { //Calling my function
    var sentence = "I am learning"; //Sentence to display
    sentence += " JavaScript"; //Sentence to display 
    document.getElementById("Text").innerHTML = sentence; //Linking my HTML Id to js so that when button clicked text changes
}